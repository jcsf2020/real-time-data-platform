[project]
name = "rtdp-contracts"
version = "0.1.0"
description = "Shared data contracts for the real-time data platform."
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.3",
]

[build-system]
requires = ["uv_build>=0.9.17,<0.10.0"]
build-backend = "uv_build"
