name: rtdp
version: '1.0.0'
config-version: 2

profile: rtdp

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  rtdp:
    silver:
      +schema: silver
      +materialized: table
    gold:
      +schema: gold
      +materialized: table
